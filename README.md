# C-Algorithms

This repository contains a collection of algorithms implemented in **C**.  
It includes common algorithmic problems, data structures, and utility functions written in pure C.

## Usage

Clone the repository and compile the C files using:

```bash
gcc filename.c -o output
./output
